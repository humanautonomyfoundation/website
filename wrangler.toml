# Cloudflare Pages project config.
# The dashboard reads pages_build_output_dir; `wrangler pages deploy` uses it too.
name = "haf-website"
compatibility_date = "2026-09-04"
pages_build_output_dir = "dist"
