// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Canonical origin. Update if the production domain changes.
  site: 'https://humanautonomyfoundation.org',
  output: 'static',
});
